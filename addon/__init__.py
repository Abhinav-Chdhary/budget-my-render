"""Blender Extension entry point for Budget My Render.

The implementation remains in ``budget_my_render.py`` so it can also be run
directly from Blender's Text Editor during development.
"""

from .budget_my_render import register, unregister
